テキストファイルから特定の文字列を検索して、一致した行を抽出するプログラムです。
一致した行一覧は新しいファイルに書き込まれます。

#必要なライブラリ
python3.2以上
chardet(pipでインストールしてください)

#使い方
extracter.pyのコメントを参考にプログラムを書き換えてください。
最初の数行を残す設定をすることができます。
このディレクトリに抽出したいファイルが入ったディレクトリを設置してください。

例
ーーextracter.py
  |
  ー抽出したいディレクトリ  ー抽出したいファイル
                          |
                          ー抽出したいファイル

extracter.pyを実行すると、hogehoge_処理済みというディレクトリが作成されて、ほしい行を抽出したファイルが作成されます。

#注意
文字化けしていたファイルは「hogehoge文字化け」というファイル名で出力します。
文字化けしていたファイルは、下記の方法でほしい行を抽出してください。

#文字化けしているファイルから必要な行を抽出する手順
①sakuraエディタやvscode等のエディタを開く（メモ帳はたぶん無理です）
②ctrl+fを押す
③正規表現で^(?!.*hogehoge).*\nを検索します。(hogehogeが含まれない行の検索をするという正規表現です。hogehogeの部分を抽出したい文字列に置き換えてください。)
④上で検索した行を全置換で消去してください（hogehogeが含まれない行が全て消されてしまうので、必要な行はコピーしておいてください。）
⑤hogehoge以外の行が全て消去されたので、コピーしていた行をペーストします。
